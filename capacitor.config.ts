import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.corporate.cafeteria',
  appName: 'CorporateCafeteria',
  webDir: 'dist'
};

export default config;
